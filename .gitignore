node_modules/
dist/
.env
*.log
.olx-pik/
.DS_Store
klijenti/
KLIJENT.md
.claude-runtime/
.claude-runtime-admin/

# Interni podaci o prospektima i tudjim shopovima: NIKAD u git, klonovi idu klijentima.
interno/
olx-dokumentacija/*.xlsx

# Sirovi ulaz saznanja iz klonova (klijentski kontekst): ne ide u git.
olx-dokumentacija/saznanja-ulaz/

# Lokalno stanje masine: instalirano/podeseno po racunaru, nikad u git.
# (launchd plistovi i Task Scheduler zadaci su ionako van repoa; ovo su njihovi tragovi u repou)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.serena/
*.pid
